# AI 抠图软件 - 技术规格说明书

## 1. 项目概述

- **项目名称**: 小飞AI抠图
- **项目类型**: 跨平台桌面应用 (macOS/Windows) + 网页版
- **核心功能**: 使用本地开源AI模型实现图片背景去除
- **目标用户**: 电商设计师、摄影师、内容创作者、普通用户

## 2. 技术架构

### 2.1 技术栈
- **桌面端**: Electron + React + TypeScript
- **后端**: Python FastAPI + PyTorch
- **AI模型**: briaai/RMBG-2.0 (BiRefNet架构)
- **模型管理**: 首次安装时自动下载模型 ~500MB

### 2.2 系统架构
```
┌─────────────────────────────────────────────────────────┐
│                    Electron App                          │
│  ┌──────────────┐    ┌──────────────────────────────┐  │
│  │   Renderer   │◄──►│      Python Backend          │  │
│  │  (React UI)  │    │    (FastAPI + RMBG-2.0)      │  │
│  └──────────────┘    └──────────────────────────────┘  │
│         │                       │                        │
│         │              ┌────────▼────────┐               │
│         │              │  AI Model      │               │
│         │              │  (本地推理)    │               │
│         │              └────────────────┘               │
└─────────────────────────────────────────────────────────┘
```

## 3. 功能规格

### 3.1 核心功能
1. **图片导入**: 支持拖拽、点击选择 (PNG, JPG, JPEG, WEBP)
2. **AI抠图**: 使用本地RMBG-2.0模型进行背景去除
3. **结果预览**: 显示原图、处理后对比
4. **图片导出**: 支持PNG透明背景、JPG带背景

### 3.2 UI设计
- 简洁现代的界面设计
- 左侧原图预览，右侧结果预览
- 顶部工具栏：导入、导出、处理按钮
- 底部状态栏：模型状态、处理进度

### 3.3 模型管理
- 首次启动自动检测模型是否存在
- 模型不存在时提示下载
- 显示模型下载进度
- 支持模型预热加速首次推理

## 4. 验收标准

- [ ] 应用可正常启动
- [ ] 可导入图片并显示预览
- [ ] AI抠图功能正常工作
- [ ] 可导出处理后的图片
- [ ] 模型自动下载功能正常
- [ ] 跨平台打包成功 (macOS/Windows)

## 5. 目录结构

```
ai-background-remover/
├── app/                      # Electron主应用
│   ├── main/                 # 主进程
│   ├── renderer/             # 渲染进程 (React)
│   └── preload/              # 预加载脚本
├── backend/                  # Python后端
│   ├── main.py               # FastAPI服务
│   ├── model.py              # RMBG模型推理
│   └── requirements.txt     # Python依赖
├── package.json
└── README.md
```
